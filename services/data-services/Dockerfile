# Data Services 包含三个独立子服务，各自拥有独立的 Dockerfile：
#   - query-api/Dockerfile   (端口 8082)
#   - audit-api/Dockerfile   (端口 8083)
#   - export-api/Dockerfile  (端口 8084)
#
# 构建示例（从 services/data-services/ 目录执行）：
#   docker build -f query-api/Dockerfile -t nexuslog/query-api .
#   docker build -f audit-api/Dockerfile -t nexuslog/audit-api .
#   docker build -f export-api/Dockerfile -t nexuslog/export-api .
