apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nexuslog-observability
  namespace: argocd
spec:
  project: nexuslog
  source:
    repoURL: "https://github.com/nexuslog/nexuslog.git"
    targetRevision: main
    path: platform/helm/nexuslog-observability
  destination:
    server: "https://kubernetes.default.svc"
    namespace: nexuslog-observability
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
