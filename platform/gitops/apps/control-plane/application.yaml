apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nexuslog-control-plane
  namespace: argocd
spec:
  project: nexuslog
  source:
    repoURL: "https://github.com/nexuslog/nexuslog.git"
    targetRevision: main
    path: platform/helm/nexuslog-control-plane
  destination:
    server: "https://kubernetes.default.svc"
    namespace: nexuslog-control-plane
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
